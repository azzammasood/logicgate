import { EmptyState } from "@/components/EmptyState";

export default function DefinitionsPage() {
  return (
    <main className="flex flex-1 items-center justify-center">
      <EmptyState variant="select-definition" />
    </main>
  );
}
